# 统一行尾，避免 Windows 上的 core.autocrlf 把 shell 脚本转成 CRLF
# （gradlew 会在 CI 的 Linux 上执行，CRLF 会导致 "bad interpreter: /bin/sh^M"）

* text=auto eol=lf

# Windows 批处理必须保持 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件不做行尾转换
*.jar binary
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.gif binary
*.apk binary
*.aab binary
*.jks binary
*.keystore binary
